# Canonical public URL used at build time (no trailing slash).
# Required for production deployments (Coolify, CI). Omit for local `npm start` if you rely on localhost.
DOCS_SITE_URL=https://docs.example.com
